# payments-service platform infrastructure

Landing zone for the payments service, managed with Terraform through HCP
Terraform (VCS-connected workspace `ai-devops-demo`). Changes land by pull
request: plans and policy checks run on the PR, and merges to `main` apply
automatically. See `AGENTS.md` for the workflow when working in this repo
with a coding agent.
